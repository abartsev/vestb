export type ThemeMode = 'light' | 'dark';

export interface ThemeColors {
  primary: string;
  success: string;
  danger: string;
  background: string;
  surface: string;
  border: string;
  text: string;
  textSecondary: string;
  textLight: string;
  icon: string;
  placeholder: string;
}
